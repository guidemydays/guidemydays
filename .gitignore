# ─────────────────────────────────────────────────────────────────────────────
# Only the WEBSITE is published to GitHub Pages. GitHub Pages republishes the
# whole repo on every build and caps the total site at ~1 GB. Everything outside
# Travel-Website/ (Icons Library ~672MB, Brain rules+scripts, archive, Hotel
# Research, shopping profile, scratch audit folders) is authoring material that
# lives on the shared Drive only — it must NEVER be published: it blows the 1 GB
# limit (failing every deploy with "Page build failed" / dur 0) and exposes
# private files on a public site.
#
# Whitelist strategy: ignore EVERY top-level entry, then re-include only the site
# (Travel-Website/) plus the deploy plumbing (.github/, .nojekyll, .gitignore).
# DEFAULT = DRIVE-ONLY: nothing is tracked/published unless the live site needs it
# to function. Any NEW top-level folder a crib creates is auto-excluded — no
# maintenance needed. Files all stay on disk; this only controls what git tracks.
#
# GOTCHA — a whitelist does NOT untrack files ALREADY in the git index: a tracked
# file overrides .gitignore. Anything committed BEFORE this whitelist existed stays
# tracked + published until you `git rm --cached` it. That is how Brain/, CLAUDE.md,
# and To Do List/ stayed published until they were untracked 2026-07-14. Audit for
# stragglers (must print nothing):
#   git ls-files | grep -vE '^Travel-Website/|^\.github/|^\.gitignore$|^\.nojekyll$'
# ─────────────────────────────────────────────────────────────────────────────
/*
!/.gitignore
!/.nojekyll
!/Travel-Website/
# CI: the Pages deploy workflow must be tracked (it gates deploys on guide validation).
# Only .github/ is re-included for tracking; it is NOT published to the site (the
# deploy job stages only Travel-Website/ + .nojekyll — see .github/workflows/deploy-pages.yml).
# .github/scripts/ held legacy CI validators not used by the workflow — untracked 2026-07-30.
# That exclusion stands, and the folder stays ignored BY DEFAULT. live_smoke.py is
# re-included by name because it is the opposite case: the deploy workflow RUNS it
# (post-deploy health check), and CI only ever sees this repo — Brain/ is tracked in
# the private Travel-Brain repo and does not exist in the Actions checkout. Tracked,
# never published: the deploy job stages only Travel-Website/ + .nojekyll.
!/.github/
/.github/scripts/*
!/.github/scripts/live_smoke.py
# Claude Code workflows (routines) — tracked so cribs share them.
# NOT published to the site (deploy job only stages Travel-Website/).
!/.claude/
!/.claude/workflows/
.claude/settings*.json
.claude/settings*.json.bak

# Guide build scaffolding INSIDE the site tree. IMPORTANT: guide HTML serves its
# photos straight out of _build/assets/ (src="_build/assets/800px-*.jpg"), so that
# ONE subfolder IS published site content and must stay tracked. Everything else in
# _build/ (verification logs, build_state, raw full-size originals) is not served —
# ignore _build/ contents but re-include _build/assets/.
**/_build/*
!**/_build/assets/

# OS / editor cruft anywhere
**/.DS_Store
**/.fuse_hidden*
*.pyc
